spring:
  jpa:
    hibernate:
      ddl-auto: update
    database: postgresql
  datasource:
    url: jdbc:postgresql://microservice.ctwe0yqymv49.us-east-1.rds.amazonaws.com:5432/microservice
    username: postgres
    password: amisimegustaeso*

eureka:
  instance:
    prefer-ip-address: true
  client:
    service-url:
      default-zone: ${EUREKA_URI:http://localhost:8761/eureka}

application-description: Its Done UK Application
application-license: API Licence
application-version: 1.0
