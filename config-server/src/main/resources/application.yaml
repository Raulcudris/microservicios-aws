spring:
  application:
    name: config-server
  profiles:
    active: git
  cloud:
    config:
      server:
        git:
          uri: https://github.com/Raulcudris/microservicios-aws.git
          username: Raulcudris
          password: ra1065808524
          search-paths: config

server:
  port: 8888

